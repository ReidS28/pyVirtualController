[project]
name = "pyvirtualcontroller"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.129.0",
    "uvicorn>=0.40.0",
    "vgamepad>=0.1.0",
]
